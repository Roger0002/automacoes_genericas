- hosts: clienterhel8.s2linux.local
  gather_facts: true
  tasks:
    - shell: yum -q repolist
      register: yum_repolist

    - shell: subscription-manager repos --list-enabled | grep -E "^Repo ID"
      register: subsc_man

    - debug:
        var: subsc_man 
